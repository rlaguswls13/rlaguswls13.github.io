---
category: "tech_study"
created_date: "2026-08-24"
tag: ["컴파일","인터프리터"]
slug: "java-compile-interpeter"
subcategory: "java"
status: "publish"
description: "Java는 기본적으로 컴파일 언어로 이해하는 것이 좋습니다. Java 소스 코드는 실행 전에 javac을 통해 바이트코드로 컴파일되며, 이러한 컴파일 과정이 명확하게 존재하기 때문입니다.\n물론 JVM 내부에서는 인터프리팅과 JIT 컴파일 등의 방식을 함께 사용하기 때문에 인터프리터 언어의 특징도 있다고 볼 수 있습니다. 하지만 기본 개념을 구분할 때는 JVM 내부 동작까지 확장하기보다 “실행 전에 컴파일하는가?”, “어느 쪽의 특성이 더 강한가?” 정도를 기준으로 정리하는 것이 혼동을 줄이는 데 도움이 됩니다."
title: "Java는 컴파일 언어인가 인터프리터 언어인가 - 하이브리드 실행 구조 분석"
page_id: "3c619946-ca76-8098-af99-c5bb8fb90bec"
source_id: "3c619946ca768098af99c5bb8fb90bec"
last_edited_time: "2026-08-24T09:58:00.000Z"
notion_url: "https://app.notion.com/p/Java-3c619946ca768098af99c5bb8fb90bec"
id: "3c619946ca768098af99c5bb8fb90bec"
sourceId: "3c619946-ca76-8098-af99-c5bb8fb90bec"
date: "2026-08-24"
tags: ["컴파일","인터프리터"]
package: "java"
notionUrl: "https://app.notion.com/p/Java-3c619946ca768098af99c5bb8fb90bec"
---

## 목차

- [핵심 요약](#핵심-요약)
- [1. 컴파일 언어 vs 인터프리터 언어 비교](#1-컴파일-언어-vs-인터프리터-언어-비교)
  - [① 컴파일 언어 (Compiled Language)](#1-컴파일-언어-compiled-language)
  - [② 인터프리터 언어 (Interpreted Language)](#2-인터프리터-언어-interpreted-language)
- [2. Java의 2단계 (Hybrid) 실행 아키텍처](#2-java의-2단계-hybrid-실행-아키텍처)
  - [1단계: 사전 컴파일 (javac)](#1단계-사전-컴파일-javac)
  - [2단계: JVM 런타임 실행 (Interpreter + JIT Compiler)](#2단계-jvm-런타임-실행-interpreter-jit-compiler)
- [3. "실행 환경(JVM)을 제외하고 언어 자체로만 보면 컴파일 언어인가?" 관점 정립](#3-실행-환경jvm을-제외하고-언어-자체로만-보면-컴파일-언어인가-관점-정립)

<a id="핵심-요약"></a>

## 핵심 요약

결론부터 말하면 **Java는 전통적인 1단계 분류법으로 단정할 수 없으며, '컴파일 방식'과 '인터프리터 방식'을 모두 사용하는 하이브리드(Hybrid / 2단계) 언어**다.

- **소스 코드 관점**: 자바 소스코드(`.java`)는 사전 컴파일러(`javac`)에 의해 자바 바이트코드(`.class`)로 컴파일되므로 **컴파일 언어**의 특성을 가진다.
- **런타임 실행 관점**: 생성된 바이트코드는 [JVM](https://app.notion.com/p/JVM%EA%B3%BC%20JRE%20-%20%EC%B0%A8%EC%9D%B4) 내부에서 **인터프리터(Interpreter)**로 해석되어 실행되다가, 자주 호출되는 코드(Hotspot)는 **JIT(Just-In-Time) 컴파일러**에 의해 기계어로 즉시 변환된다.

<NotionDivider />

<a id="1-컴파일-언어-vs-인터프리터-언어-비교"></a>

## 1. 컴파일 언어 vs 인터프리터 언어 비교


```text
[컴파일 언어]   : 소스 코드 (.c) ─────── (컴파일) ───────> 실행 파일 (기계어 / .exe) ──> 빠른 실행
[인터프리터 언어]: 소스 코드 (.py) ────── (Line-by-Line 해석) ──────────────────────────> 즉시 실행

[Java 하이브리드]: 소스 코드 (.java) ── (javac 컴파일) ──> 바이트코드 (.class) ── (JVM: Interpreter + JIT) ──> 기계어 실행
```

<a id="1-컴파일-언어-compiled-language"></a>

### ① 컴파일 언어 (Compiled Language)

- **개념**: 소스 코드 전체를 실행하기 전 컴파일러(Compiler)를 통해 해당 OS의 **타깃 기계어(Machine Code)**로 한 번에 번역하여 실행 파일(`exe`, `bin`)을 생성하는 언어 (예: C, C++, Rust, Go).
- **장점**:
- **빠른 실행 속도**: 이미 기계어로 번역되어 있어 실행 시 오버헤드가 적다.
- **사전 에러 검출 (Type Safety)**: 컴파일 시점에 문법 에러 및 타입 체크가 이루어진다.
- **코드 보안**: 소스코드가 원천 노출되지 않고 바이너리 형태로 배포된다.

<a id="2-인터프리터-언어-interpreted-language"></a>

### ② 인터프리터 언어 (Interpreted Language)

- **개념**: 사전 컴파일 과정 없이 소스 코드를 한 줄씩(Line-by-Line) 읽어가며 인터프리터가 즉시 해석하여 실행하는 언어 (예: Python, JavaScript, Ruby).
- **장점**:
- **플랫폼 독립성**: 인터프리터만 설치되어 있으면 어떤 OS에서든 소스 코드 그대로 구동된다.
- **높은 개발 생산성**: 컴파일 대기 시간이 없어 코드를 수정하고 즉시 결과를 확인할 수 있다.
- **동적 유연성**: 동적 타이핑과 코드 수정을 쉽게 적용할 수 있다.


<NotionDivider />

<a id="2-java의-2단계-hybrid-실행-아키텍처"></a>

## 2. Java의 2단계 (Hybrid) 실행 아키텍처

Java는 "Write Once, Run Anywhere (WORA)" 철학을 위해 2단계 프로세스를 취한다.

<a id="1단계-사전-컴파일-javac"></a>

### 1단계: 사전 컴파일 (`javac`)

- 자바 컴파일러(`javac`)가 `.java` 소스 코드를 읽어 **자바 바이트코드(****`.class`****)**로 변환한다.
- 이 단계는 OS 기계어로 번역하는 것이 아니며, 문법 검사 및 표식화 작업을 수행하는 **컴파일 과정**이다.
<a id="2단계-jvm-런타임-실행-interpreter-jit-compiler"></a>

### 2단계: JVM 런타임 실행 (Interpreter + JIT Compiler)

- 바이트코드는 OS가 직접 실행할 수 없으므로 [JVM(Java Virtual Machine)](https://app.notion.com/p/JVM%EA%B3%BC%20JRE%20-%20%EC%B0%A8%EC%9D%B4)이 구동된다.
- **인터프리터(Interpreter)**: 바이트코드를 한 줄씩 읽어 OS 기계어로 해석하며 구동한다 (빠른 초기 스타트).
- **JIT(Just-In-Time) 컴파일러**: 반복 실행되는 핫스팟(Hotspot) 바이트코드를 감지하여, 해당 부분을 OS 기계어로 직접 컴파일 후 카피해 두고 인터프리팅 없이 기계어로 직접 쏜다 (컴파일 언어급 실행 속도 확보).

<NotionDivider />

<a id="3-실행-환경jvm을-제외하고-언어-자체로만-보면-컴파일-언어인가-관점-정립"></a>

## 3. "실행 환경(JVM)을 제외하고 언어 자체로만 보면 컴파일 언어인가?" 관점 정립

"JVM 영역까지 고려하는 것은 언어 분류에 맞지 않다"는 시각에 대한 기술적 구분:

1. **언어 문법 및 빌드 관점 (Language Specification)**:
- 자바는 소스 코드 차원에서 문법 검사, 타입 체킹, 클래스 파일(`.class`) 생성을 위한 `javac` 컴파일 단계가 **필수적**이다. 따라서 언어의 규격상 **컴파일 언어(Compiled Language)**로 분류하는 것이 타당하다.

1. **런타임 및 구현체 관점 (Implementation Specification)**:
- 자바 언어로 작성된 바이트코드가 최종 실행되는 방식은 **인터프리터와 JIT의 결합**이다.

> **최종**:


<NotionIndent>

언어 문법 및 소스 빌드 차원에서는 **'컴파일 언어'**가 맞으며, 실행 런타임 메커니즘까지 포함하면 **'컴파일 방식을 결합한 하이브리드 인터프리팅 언어'**라고 표현하는 것이 가장 정밀한 정답이다.


</NotionIndent>

